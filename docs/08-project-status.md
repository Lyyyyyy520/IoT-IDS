# 08 - 项目进度总结

> 状态：进度快照
> 更新日期：2026-08-27
> 关联文档：[06-demo-design.md](06-demo-design.md)、[07-device-topology.md](07-device-topology.md)

## 1. 项目概述

智慧社区 IoT 僵尸网络入侵检测系统（天津理工大学大创项目）。核心是**设备图 GNN**：从"哪条流是攻击"升级到"哪台设备被感染"，配合分级响应，现场演示完整的攻防闭环。

---

## 2. 已完成部分

### 2.1 算法主线（全部完成，纯软件，零硬件）

| 模块 | 状态 | 成果 |
|------|------|------|
| 流级检测引擎（CICIDS GAT） | ✅ 完成 | F1 0.9531，已导出 ONNX |
| 实时特征提取器（流量→72维） | ✅ 完成 | 已集成后端 |
| 实时检测引擎 + 后端集成 | ✅ 完成 | 抓包管线 + 告警生成 |
| **设备图 GNN（本次新增）** | ✅ 完成 | 见下方详述 |

### 2.2 设备图 GNN 完整链路（本次核心成果）

```
CICIoT2023 数据（含 IP + 标签）
  → 抽样平衡子集（176.6 万行）
  → 建设备图（节点=设备×时间窗口，边=通信+时序，四级风险）
  → 训练 GAT（0.12M 参数）
  → 导出 ONNX（146KB，动态节点数）
```

| 指标 | 结果 |
|------|------|
| 节点数 | 6,421（设备 × 1小时窗口） |
| 特征 | 13 维（IP 无关的流量统计） |
| 模型参数 | 0.12M（极轻量） |
| 僵尸网络检测 | **F1 0.95，漏报 0** |
| 正常设备识别 | F1 0.87 |
| 拒绝服务识别 | F1 0.91 |
| 侦察识别 | F1 0.56（与正常混淆，可接受） |
| ONNX 模型 | 146KB，支持动态节点数 |

**核心安全指标**：僵尸网络从不漏报（326 样本 0 误判为正常）、正常设备几乎不误报（232 样本仅 1 误判）。

### 2.3 文档体系

| 文档 | 内容 |
|------|------|
| [06-demo-design.md](06-demo-design.md) | 现场演示方案（机器人 + 行为剧本 + 分级阻断） |
| [07-device-topology.md](07-device-topology.md) | 设备拓扑（硬件型号 + 拓扑图 + 通信设计） |
| [08-project-status.md](08-project-status.md) | 本文档（进度总结） |

---

## 3. 待完成模块

### 3.1 软件模块

| 模块 | 说明 | 依赖 |
|------|------|------|
| ① 树莓派边缘部署脚本 | ONNX Runtime 推理 + 实时抓包 + 设备图实时构建 | 树莓派 |
| ② 设备图实时构建模块 | 现场抓包 → 提取 13 维特征 → 建图（通信边+时序边） | ① |
| ③ 特征归一化 | log1p + z-score（用 `device_gnn_norm.npz`） | ② |
| ④ 分级响应执行模块 | 网络隔离（iptables）+ 物理阻断（MQTT 指令） | ② |
| ⑤ ESP32 设备固件 | 各设备模拟程序（门禁/路灯/插座/传感器/音箱）+ MQTT 遥测 | ESP32 |
| ⑥ 攻击模拟脚本 | Mirai 洪水（UDP/GRE），跑在被感染设备上 | ESP32/Pi |
| ⑦ 三端可视化 | WebSocket 实时推送 → Pi 屏 + 电脑 + APP 拓扑图 | ② |
| ⑧ 蜜罐（可选 P2） | 假 Telnet/SSH 服务诱捕真实扫描 | 树莓派 + 外网 |

### 3.2 依赖关系

```
① 树莓派部署脚本 ← ④ 响应执行 ← ② 设备图构建 ← ③ 归一化
                                    ↓
                              ⑦ 三端可视化
                                    ↓
              ⑤ ESP32 固件 ← ⑥ 攻击脚本（现场流量来源）
```

---

## 4. 硬件购置清单

### 4.1 第一批：完整演示功能所需（P0 核心闭环）

> 这批买齐就能跑通"ESP32 真实流量 + 模拟攻击 → Pi 检测"的完整演示。

| 硬件 | 型号 | 数量 | 单价 | 小计 |
|------|------|------|------|------|
| 树莓派 | 4B 4GB | 1 | — | ✅ **已有** |
| ESP32-C3 | 合宙**经典款**（有串口芯片，支持 IDE 调试） | 5 | ¥18 | ¥90 |
| ESP32-CAM | AI-Thinker + OV2640（带底板版更省事） | 1 | ¥30 | ¥30 |
| USB 转 TTL（可选） | CH340 | 1 | ¥7 | ¥7 |
| **小计** | | | | **¥120-127** |

> 说明：攻击者不需要单独买——攻击脚本跑在"被感染"的某台 ESP32 上，或直接跑在树莓派上。

### 4.2 第二批：震撼效果增强（P1 物理反馈 + 机器人）

> 这批让设备"动起来、变色、报数、说话"，演示更震撼。

| 硬件 | 型号 | 数量 | 单价 | 小计 |
|------|------|------|------|------|
| 数字孪生屏 | 5寸 HDMI LCD (800×480) 触摸屏 | 1 | ¥150 | ¥150 |
| USB 音箱 | 迷你 | 1 | ¥15 | ¥15 |
| 舵机 | SG90 9g | 3（门禁1+云台2） | ¥5 | ¥15 |
| 继电器模块 | SRD-05VDC-SL-C | 1 | ¥5 | ¥5 |
| 光敏模块 | GL5516 | 1 | ¥3 | ¥3 |
| 温湿度传感器 | DHT11 | 1 | ¥3 | ¥3 |
| 音频模块 | MAX98357A + 小喇叭 | 1 | ¥13 | ¥13 |
| 状态灯 | WS2812B RGB 灯珠 | 6 | ¥2 | ¥12 |
| 蜂鸣器 | 有源 5V | 1 | ¥2 | ¥2 |
| 沙盘材料 | 亚克力/纸板/楼模型 | 1 | ¥50 | ¥50 |
| **小计** | | | | **¥268** |

### 4.3 可选第三批（P2 创新加分）

| 硬件 | 型号 | 数量 | 单价 | 用途 |
|------|------|------|------|------|
| 蜜罐节点 | 树莓派 Pico W | 1 | ¥20 | 独立诱捕节点（需外网） |

---

## 5. 完整实现路径

```
现在（算法完成，零硬件）✅
   流级 GAT + 设备图 GNN 都已训练 + 导出 ONNX

下一步（购第一批硬件）
   → 写 ESP32 固件（模拟设备）
   → 写树莓派部署脚本（抓包 + 建图 + ONNX 推理）
   → P0 核心演示闭环跑通

再下一步（购第二批硬件）
   → 物理反馈（舵机/继电器/LED）
   → 机器人（屏 + 语音）
   → P1 震撼演示

可选（购第三批）
   → 蜜罐诱捕真实攻击
   → P2 创新加分
```

---

## 6. 进度评估

| 维度 | 进度 | 说明 |
|------|------|------|
| 算法（核心） | ✅ **100%** | 流级 + 设备级两个模型都完成 |
| 文档 | ✅ **100%** | 方案 + 拓扑 + 进度齐全 |
| 软件部署 | ⏳ 0% | 待购树莓派后写部署脚本 |
| 硬件 | ⏳ 0% | 待购置 |
| 现场演示 | ⏳ 0% | 依赖软件部署 + 硬件 |

**当前结论**：项目最难的**算法部分（设备图 GNN）已全部完成**，剩余是硬件购置 + 边缘部署 + 现场联调，属于"工程落地"阶段，风险较低。

---

## 7. 下一步行动建议

1. **购置 ESP32 板子**（ESP32-C3 经典款 ×5 + ESP32-CAM ×1，约 ¥120，树莓派已有）
2. **写树莓派部署脚本**（不依赖 ESP32 到货，可先用回放数据验证"抓包→建图→ONNX 推理"链路）
3. **写 ESP32 设备固件**（模拟门禁/路灯/插座/传感器/音箱的 MQTT 遥测）
4. 硬件到货后，**现场联调 P0**
